spring.application.name=proj10

# PostgreSQL Datasource Configuration
# Adjust connection URL, username, and password to match your local PostgreSQL server database
spring.datasource.url=jdbc:postgresql://localhost:5432/todo
spring.datasource.username=postgres
spring.datasource.password=309684

# JPA / Hibernate Configuration
# Automatically update the database schema based on Entity definitions
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

